"""Models Tests"""

"""
import pytest
from mixer.backend.django import mixer

@pytest.mark.django_db
class TestModel:
    def test_product_is_in_stock(self):
        product = mixer.blend('product_register.Product', name='exists')
        assert product.is_in_stock('exists', True)
"""
